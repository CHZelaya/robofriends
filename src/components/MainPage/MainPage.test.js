import React from 'react';
import { render, fireEvent, cleanup } from '@testing-library/react';
import MainPage from './MainPage';

afterEach(cleanup);

test("Silly test to get rid of error code", () => {

})